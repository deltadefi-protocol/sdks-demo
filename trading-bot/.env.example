# =============================================================================
# SECRETS AND ENVIRONMENT-SPECIFIC SETTINGS
# These should be kept private and not committed to version control
# =============================================================================

# DeltaDeFi API Credentials (REQUIRED)
# Get these from your DeltaDeFi account dashboard
EXCHANGE__DELTADEFI_API_KEY=your_api_key_here
EXCHANGE__TRADING_PASSWORD=your_trading_password_here

# Network is determined by SYSTEM__MODE (testnet uses staging URLs, mainnet uses production URLs)

# System Configuration (environment-specific)
SYSTEM__MODE=testnet                    # testnet, mainnet
SYSTEM__LOG_LEVEL=INFO                  # DEBUG, INFO, WARNING, ERROR
SYSTEM__DB_PATH=trading_bot.db          # Database file path

# =============================================================================
# NOTE: Trading strategy settings are now in config.yaml
# This separation keeps secrets in .env and trading parameters in version control
# =============================================================================

# GCP Deployment related Environment Variables
GCP_PROJECT_ID=GCP_PROJECT_ID
GCP_REGION=GCP_REGION
GCP_SERVICE_NAME=GCP_SERVICE_NAME
